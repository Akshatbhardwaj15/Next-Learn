const { defaultConfig } = require("next/dist/server/config-shared");

const UserIndex = () => {
    return (
        <div>
            <h1> This is a user index</h1>
        </div>
    );
};

export default UserIndex;